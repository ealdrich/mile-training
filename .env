# In production the React app is served by the Express server (same origin),
# so REACT_APP_API_URL is empty. For local development, create .env.local and
# set REACT_APP_API_URL=http://localhost:3001
REACT_APP_API_URL=